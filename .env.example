DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Opcional: só necessário se for usar /api/admin/seed para popular os dados
# de demonstração sem acesso local ao banco (ver README). Escolha qualquer
# valor secreto — é a "senha" para chamar essa rota uma única vez.
SEED_TOKEN=""
