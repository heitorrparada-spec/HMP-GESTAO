services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: hmp
      POSTGRES_PASSWORD: hmp
      POSTGRES_DB: hmp_os
    ports:
      - "5432:5432"
    volumes:
      - hmp_os_pgdata:/var/lib/postgresql/data

volumes:
  hmp_os_pgdata:
